specVersion: "v1beta"
serviceId: "se"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "se-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
